import { checkInsCheckInModel } from './check-ins-check-in-model';

describe('checkInsCheckInModel', () => {
  it('should work', () => {
    expect(checkInsCheckInModel()).toEqual('check-ins-check-in-model');
  });
});
